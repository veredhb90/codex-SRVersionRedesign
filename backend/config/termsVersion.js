// Single source of truth for the mandatory Terms & Conditions / risk
// disclaimer version. Bump this string whenever the terms text changes
// (frontend/js/terms.js holds the matching EN/AR/HE content) to force
// every user — including ones who already signed an older version — to
// read and re-sign before they can use the site again.
module.exports = { TERMS_VERSION: '1.0' };
